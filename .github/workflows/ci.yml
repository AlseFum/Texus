name: CI

on:
  push:
    branches: [ main, master ]
  pull_request:
    branches: [ main, master ]

jobs:
  python:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'
      - name: Install uv
        run: |
          curl -LsSf https://astral.sh/uv/install.sh | sh
          echo "$HOME/.local/bin" >> $GITHUB_PATH
      - name: Sync deps
        run: uv sync --frozen --no-cache
      - name: Ruff check
        run: uv run ruff check src
      - name: Ruff format (check)
        run: uv run ruff format --check src

  frontend:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: src/Express/menu
    steps:
      - uses: actions/checkout@v4
      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
          cache-dependency-path: src/Express/menu/package.json
      - name: Install and build
        run: |
          if [ -f package.json ]; then npm ci && npm run build; else echo "no frontend"; fi

